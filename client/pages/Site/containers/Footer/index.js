/**
 * Module dependencies
 */
const React = require('react');
const PropTypes = require('prop-types');

/**
 * Footer Containers
 */
const Footer = ({}) => (
  <footer>
    footer
  </footer>
);

Footer.propTypes = {

};

Footer.defaultProps = {

};

module.exports = Footer;
