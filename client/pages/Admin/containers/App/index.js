/**
 * Module dependencies
 */
const React = require('react');
const PropTypes = require('prop-types');

/**
 * App Containers
 */
const App = ({}) => (
  <div>
    Admin
  </div>
);

App.propTypes = {

};

App.defaultProps = {

};

module.exports = App;
